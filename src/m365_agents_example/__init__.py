"""Microsoft 365 Agents SDK example - declarative manifest + typed handlers.

Default backend is an in-process stub so the kit runs anywhere without a
Bot Framework registration. Set M365_AGENT_PROVIDER=sdk (with botbuilder-core
installed) to route through the real M365 Agents SDK / Azure Bot Service.
"""
__version__ = "1.0.0"
